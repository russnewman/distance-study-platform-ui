server:
  port: 8081
  url: ${UI_URL:http://localhost:8081}

rest:
  url: ${REST_URL:http://localhost:8080}

spring:
  mvc:
    static-path-pattern: /static/**

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
